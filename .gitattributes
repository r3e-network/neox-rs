book/cli/**/*.md linguist-vendored
book/cli/cli.md -linguist-vendored

crates/storage/libmdbx-rs/mdbx-sys/libmdbx/** linguist-vendored

bun.lock linguist-language=JSON-with-Comments

# Shell scripts are only ever executed by POSIX shells. Without this, a Windows checkout with
# core.autocrlf=true turns them into CRLF and every `bash <script>` fails on `$'\r'`.
*.sh text eol=lf
