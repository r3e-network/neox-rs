# 审计概览

已完成 Neo X Rust 实现与 Neo X Geth oracle 的全量协议面静态审计，覆盖链参数/genesis、dBFT header extra、EVM/系统合约、Policy/交易池、Anti-MEV/TPKE、DKG、BEACON/2、dBFT/0 与同步/Engine API。

已修复并提交 Shanghai 激活门控下的 withdrawals_root 校验偏差、Beacon GetBlobs TTL、RPC simulation Policy 语义、同步活性与传播队列处理；相关代码和审计文档已推送至 `origin/neox`，完整审计报告位于 `docs/neox/reports/2026-09-01-FULL-AUDIT.md`。

本轮继续收敛审计结论：保留已验证 Reth 基线 `3bc71d43f7`；远端实时 Reth `main` 已前进到 `0b3475a83e07`，相对前一 tip 增加 4 个提交、12 个文件（483 insertions / 156 deletions），涉及 BAL 下载、BlockBuffer 保留 access list、Overlay multiproof v2、blob sidecar permit 与 bad block 日志。已保存到 `refs/audit/reth-main-20260902`，无冲突 merge rehearsal 树为 `661a569b35`，临时测试合并提交为 `2859683d95`。新合并树的 Neo X 核心测试为 134 passed / 0 failed（Anti-MEV/DKG 45、consensus-engine 14、EVM 28、network 47），核心严格 clippy 通过，但尚不更新 pinned baseline。受影响 workspace 复跑结果为 `reth-chain-state` 33/33、`reth-downloaders` 82/82、`reth-engine-primitives` 16/16 通过；`reth-engine-tree` 169 项中 168 通过，唯一失败仍为 `persistence::tests::test_read_only_consistency_across_reorg`，在 `persistence.rs:746` 命中 Windows MDBX `Disconnect(Os error 1224)`。该失败属于通用持久化/Windows user-mapped section 生命周期阻塞，未形成 Neo X 状态根或协议断言差异，故不计为完整 workspace 通过。2026-09-02 已用同环境 A/B 对照（同一 `target` 与环境变量，仅切换补丁）验证：针对 static file mmap 缓存的本地修复尝试对该门禁无效，打补丁前后均为 `persistence` 13 passed / 1 failed、同一 `persistence.rs:744`、同一 `Disconnect(Os error 1224)`；补丁已全部还原，未提交，vendored Reth 源码与 pinned baseline 未改动。根因收敛为该测试在同一数据目录另开只读 MDBX 环境、primary unwind 需收缩数据库文件而 Windows 禁止截断仍被映射的文件，属 MDBX + Windows 平台限制。已进一步证明归属：`persistence.rs` 与 `static_file/` 目录相对上游 pinned 基线 `3bc71d43f7` 的 `git diff` 均为空，即失败路径是未经 Neo X 改动的纯上游代码，不是 Neo X 引入的回归。主仓库 HEAD 的 `reth-engine-tree --lib` 完整计数为 166 项、165 passed / 1 failed（勿与 tip 合并树的 169 项混用）。Linux 交叉验证在本机无法执行（`wsl.exe` 被安全策略黑名单拦截、无 Docker），记为未完成。Neo X 脚本门禁 45 项通过；差分脚本语法、genesis JSON/TOML 解析与 git diff 检查通过。

2026-09-02 另完成 Anti-MEV 跨实现固定向量的离线验证：在参考客户端基准提交 `f0e2368` 的 `antimev` 包内新增导出器（未改动其任何既有文件，已逐文件比对确认 `crypto/tpke/` 18/18、`antimev/` 既有 9/9 与基准提交逐字节相同），导出 7 节点 / 5 门限 DKG 夹具的全部中间值，Rust 侧据此新增 9 项正向量与 14 项负向量集成测试。结果 `reth-neox-antimev` 全量 68 通过 / 0 失败（45 原有单元 + 23 新增），clippy `--all-targets --all-features` 0 警告，nightly rustfmt 干净，协议实现代码零改动。已验证一致：committee scaler 360、Envelope 布局常量、全局公钥推导、逐参与方公私钥份额、解密份额 7/7 逐字节一致、5-of-7 门限解密恢复相同 AES 密钥与明文、子集无关性。此前悬置的 G2 压缩字节序风险已解除——参考客户端导出的 G2 生成元压缩编码与 BLS12-381 的 IETF/blst 标准值逐字节相同，gnark-crypto 与 blst 的 G2 压缩格式兼容。同时确证一处真实分歧：参考客户端 `pkcs7UnPadding` 接受填充长度为 0 或大于块大小、且不校验填充字节重复声明值（实测三种构造分别被接受并返回 128/108/120 字节），Rust 三者均拒绝；分歧方向为 Rust 更严格，属安全加固，但链上可达性未验证，严重度记为中。附带确认两实现的配对校验均只绑定随机承诺、不绑定加密消息 `M`，行为一致。current/previous 混合与 fallback（reshare）路径仍未覆盖。

单高度 RPC 门禁因本机 `127.0.0.1:8545` 返回 HTTP 502 阻塞，未计为通过；活体 mixed-peer、DKG、fresh sync、重启/崩溃恢复和 controlled reorg 仍未完成，因此不能宣称与 Neo X Geth 达到 100% 已证明一致。
