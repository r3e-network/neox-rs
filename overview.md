# 审计概览

已完成 Neo X Rust 实现与 Neo X Geth oracle 的全量协议面静态审计，覆盖链参数/genesis、dBFT header extra、EVM/系统合约、Policy/交易池、Anti-MEV/TPKE、DKG、BEACON/2、dBFT/0 与同步/Engine API。

已修复并提交 Shanghai 激活门控下的 withdrawals_root 校验偏差、Beacon GetBlobs TTL、RPC simulation Policy 语义、同步活性与传播队列处理；相关代码和审计文档已推送至 `origin/neox`，完整审计报告位于 `docs/neox/reports/2026-09-01-FULL-AUDIT.md`。

本轮继续收敛审计结论：保留已验证 Reth 基线 `3bc71d43f7`；远端实时 Reth `main` 已前进到 `0b3475a83e07`，相对前一 tip 增加 4 个提交、12 个文件（483 insertions / 156 deletions），涉及 BAL 下载、BlockBuffer 保留 access list、Overlay multiproof v2、blob sidecar permit 与 bad block 日志。已保存到 `refs/audit/reth-main-20260902`，无冲突 merge rehearsal 树为 `661a569b35`，临时测试合并提交为 `2859683d95`。新合并树的 Neo X 核心测试为 134 passed / 0 failed（Anti-MEV/DKG 45、consensus-engine 14、EVM 28、network 47），核心严格 clippy 通过，但尚不更新 pinned baseline。受影响 workspace 复跑结果为 `reth-chain-state` 33/33、`reth-downloaders` 82/82、`reth-engine-primitives` 16/16 通过；`reth-engine-tree` 169 项中 168 通过，唯一失败仍为 `persistence::tests::test_read_only_consistency_across_reorg`，在 `persistence.rs:746` 命中 Windows MDBX `Disconnect(Os error 1224)`。该失败属于通用持久化/Windows user-mapped section 生命周期阻塞，未形成 Neo X 状态根或协议断言差异，故不计为完整 workspace 通过。2026-09-02 已用同环境 A/B 对照（同一 `target` 与环境变量，仅切换补丁）验证：针对 static file mmap 缓存的本地修复尝试对该门禁无效，打补丁前后均为 `persistence` 13 passed / 1 failed、同一 `persistence.rs:744`、同一 `Disconnect(Os error 1224)`；补丁已全部还原，未提交，vendored Reth 源码与 pinned baseline 未改动。根因收敛为该测试在同一数据目录另开只读 MDBX 环境、primary unwind 需收缩数据库文件而 Windows 禁止截断仍被映射的文件，属 MDBX + Windows 平台限制。Neo X 脚本门禁 45 项通过；差分脚本语法、genesis JSON/TOML 解析与 git diff 检查通过。单高度 RPC 门禁因本机 `127.0.0.1:8545` 返回 HTTP 502 阻塞，未计为通过；活体 mixed-peer、DKG、fresh sync、重启/崩溃恢复和 controlled reorg 仍未完成，因此不能宣称与 Neo X Geth 达到 100% 已证明一致。
