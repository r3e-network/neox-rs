# 审计概览

已完成 Neo X Rust 实现与 Neo X Geth oracle 的全量协议面静态审计，覆盖链参数/genesis、dBFT header extra、EVM/系统合约、Policy/交易池、Anti-MEV/TPKE、DKG、BEACON/2、dBFT/0 与同步/Engine API。

已修复并提交 Shanghai 激活门控下的 withdrawals_root 校验偏差、Beacon GetBlobs TTL、RPC simulation Policy 语义、同步活性与传播队列处理；相关代码和审计文档已推送至 `origin/neox`，完整审计报告位于 `docs/neox/reports/2026-09-01-FULL-AUDIT.md`。

本轮继续收敛审计结论：保留已验证 Reth 基线 `3bc71d43f7`；远端实时 Reth `main` 已前进到 `0b3475a83e07`，相对前一 tip 增加 4 个提交、12 个文件（483 insertions / 156 deletions），涉及 BAL 下载、BlockBuffer 保留 access list、Overlay multiproof v2、blob sidecar permit 与 bad block 日志。已保存到 `refs/audit/reth-main-20260902`，无冲突 merge rehearsal 树为 `661a569b35`，临时测试合并提交为 `2859683d95`。新合并树的 Neo X 核心测试为 134 passed / 0 failed（Anti-MEV/DKG 45、consensus-engine 14、EVM 28、network 47），核心严格 clippy 通过，但尚不更新 pinned baseline。受影响 workspace 复跑结果为 `reth-chain-state` 33/33、`reth-downloaders` 82/82、`reth-engine-primitives` 16/16 通过；`reth-engine-tree` 169 项中 168 通过，唯一失败仍为 `persistence::tests::test_read_only_consistency_across_reorg`，在 `persistence.rs:746` 命中 Windows MDBX `Disconnect(Os error 1224)`。该失败属于通用持久化/Windows user-mapped section 生命周期阻塞，未形成 Neo X 状态根或协议断言差异，故不计为完整 workspace 通过。2026-09-02 已用同环境 A/B 对照（同一 `target` 与环境变量，仅切换补丁）验证：针对 static file mmap 缓存的本地修复尝试对该门禁无效，打补丁前后均为 `persistence` 13 passed / 1 failed、同一 `persistence.rs:744`、同一 `Disconnect(Os error 1224)`；补丁已全部还原，未提交，vendored Reth 源码与 pinned baseline 未改动。根因收敛为该测试在同一数据目录另开只读 MDBX 环境、primary unwind 需收缩数据库文件而 Windows 禁止截断仍被映射的文件，属 MDBX + Windows 平台限制。已进一步证明归属：`persistence.rs` 与 `static_file/` 目录相对上游 pinned 基线 `3bc71d43f7` 的 `git diff` 均为空，即失败路径是未经 Neo X 改动的纯上游代码，不是 Neo X 引入的回归。主仓库 HEAD 的 `reth-engine-tree --lib` 完整计数为 166 项、165 passed / 1 failed（勿与 tip 合并树的 169 项混用）。Linux 交叉验证已在 WSL Ubuntu 完成 targeted persistence 复核：persistence 选择器 14/14 通过，重点 `test_read_only_consistency_across_reorg` targeted exact 1/1 通过；首轮一次性 `primary: signer must exist at block 1` 失败随后未复现，具体根因未证实，不据此宣称完整 workspace/live 门禁。Neo X 脚本门禁 45 项通过；差分脚本语法、genesis JSON/TOML 解析与 git diff 检查通过。

2026-09-02 另完成 Anti-MEV 跨实现固定向量的离线验证：在参考客户端基准提交 `f0e2368` 的 `antimev` 包内新增导出器（未改动其任何既有文件；已按 `git ls-tree -r FETCH_HEAD` 清单逐字节比对确认 `crypto/tpke/` 16/16、`antimev/` 既有 10/10、合计 26/26 与基准提交一致——此前记录的 18/18、9/9 为人工统计错误，已更正），导出 7 节点 / 5 门限 DKG 夹具的全部中间值，Rust 侧据此新增 9 项正向量、14 项负向量、16 项 current/previous 轮次分离向量与 4 项 PKCS#7 可达性集成测试。结果 `reth-neox-antimev` 全量 **91 通过 / 0 失败**（45 原有单元 + 46 新增，含同日追加的 3 项密文准入向量），clippy `--all-targets --all-features` 在 Neo X 代码上 **0 警告**（唯一剩余警告位于未改动的 vendored `crates/storage/libmdbx-rs/src/transaction.rs:236`），nightly rustfmt 对本次改动文件干净，协议实现代码零改动。`reth-neox-node --lib` 159 通过 / 0 失败（含同日新增的 3 项：交易池准入拒绝、轮次过滤器全表比对、轮 0 永不准入）。已验证一致：committee scaler 360、Envelope 布局常量、全局公钥推导、逐参与方公私钥份额、解密份额 7/7 逐字节一致、5-of-7 门限解密恢复相同 AES 密钥与明文、子集无关性。此前悬置的 G2 压缩字节序风险已解除——参考客户端导出的 G2 生成元压缩编码与 BLS12-381 的 IETF/blst 标准值逐字节相同，gnark-crypto 与 blst 的 G2 压缩格式兼容。同时确证一处真实分歧：参考客户端 `pkcs7UnPadding` 接受填充长度为 0 或大于块大小、且不校验填充字节重复声明值（实测三种构造分别被接受并返回 128/108/120 字节），Rust 三者均拒绝。**该分歧的链上可达性已于同日构造性证明，严重度由「中」上调为「高」**：Envelope 封装方自行挑选 AES 密钥点与随机性，故完全掌握 AES 密钥、可任意决定明文每个字节，无需委员会配合。已实际构造出畸形填充 Envelope（112 字节真实交易 + 32 字节尾部、末字节 `0x20`）并走通参考客户端真实链路——`AggregateAndDecryptWithShare` 返回非 nil 且与内层交易逐字节相等，`UnmarshalBinary` 成功、哈希与发送方均正确恢复；Rust 侧对照证明份额聚合**成功**（两者在解填充之前每一步都一致）而 `decrypt_message` 返回 `InvalidPkcs7Padding`，且那 112 字节确为可解码、可往返编码、哈希自洽的规范 EIP-2718 交易。后果是共识分叉：`consensus/dbft/dbft.go` 在解密失败时回退为「按原样执行 Envelope」，而 Geth 走完全部校验后执行内层交易，同一槽位两笔不同交易导致区块哈希分歧。残余不确定性仅两点——`validateDecryptedTx` 未能端到端执行（需完整 chain backend，但其比较项 nonce/发送方/`encrypted_hash`/gas 均在攻击者可填的明文段，属代码论证而非执行验证），以及**修复方案尚未实施、协议代码未改动，风险仍在**。修复方向为 Rust 复刻参考客户端的解填充规则，或推动其收紧；两实现对齐前不应在混合客户端网络中运行。附带确认两实现的配对校验均只绑定随机承诺、不绑定加密消息 `M`，行为一致。

**同日确证第二处分歧并更正此前的错误结论**：本文件与审计报告此前写「`CipherText.Verify()` 与 Rust `verify()` 语义一致、该对应检查确实存在」——它在"函数已定义"层面成立（`crypto/tpke/encryption.go:77`），但**参考客户端从不调用它**：对 `antimev/`、`consensus/`、`core/`、`crypto/tpke/` 全树检索 `.Verify()` 的非测试调用点为**零**。Envelope 准入实际只有三道闸——`IsEnvelope`（类型/地址/长度/前缀）、`decodeEnvelopeData` → `FromBytes`（反序列化，隐式 on-curve + in-subgroup，无配对校验）、txpool（仅 gas 与 fee）；Rust 在交易池准入与提案解析两处调用 `verify()`。已用两侧共 8 个测试构造性证明：把合法密文的 `R` 槽平移 `R + g1`（一次点加法，无需任何私钥或委员会配合），结果参考客户端照收（`IsEnvelope` 为真、`FromBytes` 成功、真实解析路径 `SetTransactions` → `decodeEnvelopeData` 通过），但 7/7 share 投入后 C(7,5)=21 个法定组合**全部**失败；Rust 侧反序列化与参考客户端一致（故是**校验**差异而非**解析**差异），交易池以 `InvalidEnvelopeCiphertext` 永久拒绝。**后果是活性停滞而非状态分叉**（此处更正我早先的"链分裂"判断）：`AggregateAndDecrypt` 校验 `e(PK,commitment)·e(rpk,g2)` 与 `Verify` 的 `e(R,g2)·e(g1,commitment)` 互为等价条件，故不存在"参考客户端能解密、Rust 拒绝"的输入；真实链路是 `dbft.go:1129-1141` 返回 error → `nspcc-dev/dbft` `check.go:79-85` 仅 `return` 并"等待更多 PreCommit"→ 而更多 share 永远无法挽救 → **该高度永久停滞**，形成 Geth 主节点卡死、Rust 主节点继续出块的单向活性风险。严重度 **高**，触发成本极低。**本轮未改动任何协议代码，风险仍在。** 另确认 Envelope 轮次过滤器（`preblock.go:145`，`d.dkgRound < min(1, b.dkgRound-1) || b.dkgRound < d.dkgRound`）虽注释写"current/previous"，但因两操作数均为 `uint32` 且用内置 `min`（非 `max`），实际接受**任意更早轮次**的 Envelope，且 `SetData` 把非当前轮全部归入 previous 桶、`aggregateAndDecrypt` 对整批做一次配对校验，故一个旧轮次的不可解密 Envelope 会污染整桶。Rust 复刻了同一过滤器（接受集逐项相同，**不是共识分歧**，任一侧单独"修正"都会立刻造成网络分裂），两侧已各加表驱动测试（主动轮 0..=12 × Envelope 轮 1..=12 全表）使修改会测试失败而非静默失步。

**current/previous 混合与 fallback（reshare）已于同日在密码学层关闭**：导出器把第一轮聚合承诺作为 `lastRoundCmt` 回传 `OnEpochChange`，一次导出 `shared` 与 `reshared` 两个密钥组的完整材料（两轮全局公钥确证不同，分离断言非平凡）。Rust 侧 16/16 通过：previous 轮 Envelope 由 previous 组、current 轮由 current 组分别开启成功；跨轮组合（previous 密文 + current 份额、current 密文 + previous 份额、previous 密文 + previous 份额但用 current 全局公钥、混合两轮的 3+2 法定人数）全部被拒绝，且拒绝发生在配对校验层 `InvalidDecryptionShares` 而非依赖下层 AES 解填充失败；fallback 路径未因 resharing 放宽门限（previous 轮 4/5 份额同样被拒绝）。

**同日另做了系统性排查（4 个零调用点，仅 1 个是真分歧）**：`CipherText.Verify()` 的零调用提示这可能是一种模式，故对 `antimev/`、`consensus/dbft/`、`crypto/tpke/`、`core/` 内全部 `Verify*`/`Validate*`/`Check*`/`IsValid*` 函数做了非测试调用点计数并全仓库复核。除上面那项外，Anti-MEV/TPKE 面上另有 3 个零调用点，但**都不是分歧**：`PVSS.VerifyCommitment()`（`crypto/tpke/pvss.go:90`）与 `PVSS.VerifyRenovate()`（`:121`）属有意委托——`antimev/keygroup.go:22` 记载"PVSS is verified in DKG contract"，且 `consensus/dbft/dkg.go:995` 的 `getSharePVSS` 证实所有 PVSS 都经 `readFromContract` 从 `KeyManagementProxyHash` 的 `spvsses(round,index)` 读回，即消费的是**合约已接受的产物**；`PublicKey.VerifySigShare()`（`crypto/tpke/public_key.go:117`）则属冗余——`antimev/signature.go:36` 的 `aggregateAndVerifySig` 遍历全部 C(n,t) 组合、对每组聚合后调用 `globalPubKey.VerifySig` 校验聚合签名，逐份额校验能发现的问题它同样能发现。Rust 在 `dkg.rs:553` 的 `DkgPvss` 解码构造函数末尾执行 `pvss.verify()?`，两项检查与 Geth `VerifyCommitment()` 逐项对应，属纵深防御。**由此产生一项新的重要开放项**：Reth 在解码时强制校验，意味着若链上 canonical PVSS 不满足该校验，后果不是"拒绝一笔交易"而是**无法构建本轮 keystore、卡在 DKG 层**；所以链上 DKG 合约的 PVSS 校验强度直接决定 Reth 能否跟上主网（合约强于或等于 Reth → 无差异；合约弱于 Reth → Geth 正常而 Reth DKG 层活性故障）。合约源码不在任一仓库内，参考客户端只 vendor 了只读 getter ABI 与写入用最小 ABI，均无法反映合约内部逻辑，本机又无节点，故**该项无法在本轮关闭**。

**调度层已于同日关闭，但仅限静态分析 + 单元测试，不是活体门禁**：Geth `consensus/dbft/dkg.go:186-189` 的四个高度检查点（`targetHeight = EpochStartHeight + epochDuration`、`shareStartHeight = targetHeight - 2*sharePeriodDuration`、`recoverStartHeight = shareStartHeight + sharePeriodDuration`、`recoverCheckHeight = recoverStartHeight + sharePeriodDuration/2`）与 Rust `crates/neox/node/src/dkg.rs:50-72` 的 `DkgSchedule::new` **公式逐项一致**，Rust 侧 6/6 测试通过。架构上 Geth 是增量式高度驱动状态机（`handleDKG` 由 `dbft.go:1613/:2171/:2787` 调用），Rust 是链上 canonical 状态的幂等重放（`dkg_replay.rs`），**重放在重启与追赶路径上更严格**（Geth 的 `if snapshot.Round > 2` 意味着 round ≤ 2 时追赶路径不读 `Round-2` 承诺，Rust 则以显式 `RoundMismatch` 拒绝），属 Rust 更保守的方向，不构成共识分歧。`OnEpochChange` 的触发条件、`lastCommitment` 来源与 `RevertRound`/`Reset` 语义已逐项对照。**仍未覆盖**：链上 KeyManagement 契约对 `lastRoundCmt` 的提交权限判定与错误 round 罚没（静态阅读确认 Geth 侧以 `CurrentCNs`/`PendingCNs` 成员资格为门槛，已读路径中未见罚没逻辑，但这是"已读路径未见"而非"全树不存在"）。

单高度 RPC 门禁因本机 `127.0.0.1:8545` 返回 HTTP 502 阻塞，未计为通过；活体 mixed-peer、DKG epoch、fresh sync、重启/崩溃恢复和 controlled reorg 仍未完成，因此不能宣称与 Neo X Geth 达到 100% 已证明一致。

**工具链与门禁口径（诚实标注）**：本仓库 `rustfmt.toml` 依赖 nightly 专有选项，而 `nightly-2026-08-27` 与 `nightly-2026-08-28` 两个已安装工具链**均未附带 rustfmt 组件**（`rustup` 回落到 stable rustfmt，忽略全部不稳定选项后产生 2389 处伪差异）；只有 `nightly` 别名（`rustfmt 1.10.0-nightly (e457a7b0d3 2026-08-27)`）可用。在该工具链下，**本次改动的两个文件已格式化干净**，但 HEAD 上另有 4 个与本次无关的文件共 5 处既有格式差异（`crates/ethereum/node/tests/e2e/utils.rs:73`、`crates/neox/evm/src/factory.rs:302`、`crates/neox/node/src/sync/sidecar.rs:155` 与 `:823`、`crates/neox/node/src/sync.rs:2831`）——本轮**未**改动它们（属协议实现代码，超出本次审计范围），故全仓库 `--all --check` 仍返回非零。
